# Production Config (DataSource is dynamically managed by DataSourceConfig.java)
# Supports PostgreSQL with automated postgres:// to jdbc:postgresql:// translation
# and graceful fallback to persistent H2 if external database is unavailable.
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=false

